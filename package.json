{
  "name": "aoc",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "up": "bun update && dprint config update",
    "lint": "oxlint --deny-warnings && tsc && dprint check"
  },
  "devDependencies": {
    "bun-types": "*",
    "dprint": "*",
    "oxlint": "*",
    "typescript": "*",
    "vitest": "*"
  }
}
